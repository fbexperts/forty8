# Meta (Facebook) Pixel for forty8.co.
# Optional — only needed to OVERRIDE the committed default (LIXFIX's Pixel, 738378879546409).
# Set this to point the site at a different pixel without a code change.
PUBLIC_META_PIXEL_ID=
